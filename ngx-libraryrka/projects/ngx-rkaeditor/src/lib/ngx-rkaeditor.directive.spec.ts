import { NgxRkaeditorDirective } from './ngx-rkaeditor.directive';

describe('NgxRkaeditorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxRkaeditorDirective();
    expect(directive).toBeTruthy();
  });
});
